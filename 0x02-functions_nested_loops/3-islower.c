#include"main.h"
/**
 *_islower-check if character is lowercase.
 *Return: 0-if _islower, 1-if not.
 *let c be my variable
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
{
	return (1);
}
else
{
	return (0);
}
}

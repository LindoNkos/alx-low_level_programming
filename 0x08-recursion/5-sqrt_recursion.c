#include "main.h"
/**
 * _sqr_reecursion - evaluate function sqrt
 * @i: integer
 * @n: integer
 * Return: evaluate sqrt
 */
int _sqrt_recursion(int n)
{
	if (n == 0 || n == 1)
		return (n);
	else if (i * i == n)
		return (i);

	return (-1);

	return (-1);
}
